<div class="well bs-component">
  <%= form_for @message, remote: true do |f| %>
    <%= f.hidden_field :receiver_id, value: @user.id %>
    <%= f.text_area :content, id: 'pm-content' %>

    <%= f.submit t(:send_pm) %>
  <% end %>
</div>