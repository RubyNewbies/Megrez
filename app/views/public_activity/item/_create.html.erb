<% if activity.trackable && activity.trackable.course_id == @course.id %>
  <div class="qa-message-list" id="wallmessages">
    <div class="message-item" id="m16">
      <div class="message-inner">
        <div class="message-head clearfix">
          <div class="user-detail">
            <h5 class="handle">
              <%= link_to activity.owner.realname, profile_path(activity.owner.username) if activity.owner %>
              <%= t :created_item %>
              <%= link_to activity.trackable.name, course_items_path(course_id: activity.trackable.course_id) %>
              <%= t :at %>
              <%= link_to @course.full_name, course_path(id: @course.id) %>
            </h5>
          </div>
        </div>
        <div class="qa-message-content">
          <%= render partial: 'public_activity/time.html.erb', locals: {dest: activity} %>
        </div>
      </div>
    </div>
  </div>
<% end %>