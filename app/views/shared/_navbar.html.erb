<div class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <a href="/" class="navbar-brand"><%= t(:megrez) %></a>
        </div>
        <ul class="nav navbar-nav"><li><a href="/courses"><%= t(:all_courses) %></a></li></ul>

        <% if current_user %>
          <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="message"><i class="fa fa-twitch fa-lg"></i></a>
              <ul class="dropdown-menu" aria-labelledby="download">
                <li><a href=""><%= t(:no_messages) %></a></li>
              </ul>
            </li>

            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="download"><strong><%= current_user.username %></strong><span class="caret"></span></a>
              <ul class="dropdown-menu" aria-labelledby="download">
                <li>
                  <div style="width: 300px;padding: 10px">
                    <div class="row">
                      <div class="col-md-4">
                        <p class="text-center">
                          <%= image_tag current_user.avatar, width: 100, height: 100 %>
                        </p>
                      </div>
                      <div class="col-md-8">
                        <p class="test-left"><%= current_user.username %></p>
                        <p class"test-left"><%= current_user.realname %></p>
                        <p class"test-left"><%= current_user.email %></p>
                        <p class"test-left">
                          <a href="<%= edit_user_path(id: current_user.id) %>" class="btn btn-primary btn-block btn-sm"><%= t(:edit_profile) %></a>
                        </p>
                      </div>
                    </div>
                  </div>
                </li>
                <hr style="margin-top: -10px; margin-bottom: 5px">
                <li><a href="/courses/new" style="color:red"><%= t(:create_course) %></a></li>
                <li><a href="/dashboard"><%= t(:dashboard) %></a></li>
                <li><a href="/logout"data-method="delete"><%= t(:logout) %></a></li>
              </ul>
            </li>
          </ul>
        <% end %>
        </div>
      </div>
    </div>